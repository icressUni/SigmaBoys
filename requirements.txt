# Face recognition library
face_recognition==1.3.0

# Dependencies for face_recognition (automatically installed but listed for clarity)
dlib==19.24.9
numpy==1.24.4
Pillow==10.4.0
click==8.1.8

# Note: 
# - os and pickle are built-in Python modules, no installation needed
# - face_recognition requires system dependencies in Docker:
#   - cmake, build-essential, libopenblas-dev, liblapack-dev, libx11-dev, libgtk-3-dev